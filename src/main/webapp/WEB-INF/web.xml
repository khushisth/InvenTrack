<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <!--
      Registers the AuthFilter for all requests.
      The filter handles:
      - redirecting unauthenticated users to login
      - allowing public routes (login/register) and static assets
      - enforcing role-based restrictions for select routes
    -->
    <filter>
        <filter-name>AuthFilter</filter-name>
        <filter-class>com.inventrack.util.AuthFilter</filter-class>
    </filter>

    <filter-mapping>
        <filter-name>AuthFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

</web-app>
